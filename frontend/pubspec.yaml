name: song_downloder
description: "Personal high-quality audio downloader."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  path_provider: ^2.1.4
  shared_preferences: ^2.3.2
  audioplayers: ^6.1.0
  just_audio: ^0.10.4
  audio_service: ^0.18.18
  audio_session: ^0.2.2
  intl: ^0.19.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
